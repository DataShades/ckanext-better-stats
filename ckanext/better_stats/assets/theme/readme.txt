How to compile styles?

https://www.npmjs.com/package/sass

Usage:

1. Install with `npm install -g npx sass`
2. Use `npx sass --watch styles.scss:./../css/style.css` for development.
3. Use `npx sass styles.scss:./../css/style.css -s compressed --no-source-map` for production.
